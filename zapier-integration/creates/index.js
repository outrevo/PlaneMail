const addSubscriber = require('./addSubscriber');
const updateSubscriber = require('./updateSubscriber');
const createSegment = require('./createSegment');
const tagSubscriber = require('./tagSubscriber');
const untagSubscriber = require('./untagSubscriber');

module.exports = {
  addSubscriber,
  updateSubscriber,
  createSegment,
  tagSubscriber,
  untagSubscriber,
};
